name: CI
on: [push, pull_request]

jobs:
  check:                  # ← this must be indented under jobs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo checks
        run: |
          echo "Running basic checks"
          python -V || true
          echo "OK"
